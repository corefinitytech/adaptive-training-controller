"""Hydra + Pydantic experiment configuration (composition, validation, resolved-config
capture for reproducibility).

Not yet implemented. Phase 1 constructs `ActionSpace`/`BudgetUsage`/etc. directly in
Python; this subpackage lands once `examples/first_experiment` needs YAML-composed,
multi-run configs, per the project's build-sequence plan (`docs/adr/`).
"""
