"""The Jev (TypeSafe AI) controller: translates the action space into Jev's choice/
score/noul questions and validated answers back into a typed `Action`.

Not yet implemented — lands in Phase 2, after the deterministic baseline controllers are
proven end to end, per the research spec's own binding instruction (§17) and the
project's build-sequence plan (`docs/adr/`). The first task of that phase is verifying
`typesafe-sdk`'s real interface against `docs.typesafe.ai` — never guessing it.
"""
